'use strict'

const expect = require('chai').expect;
const assert = require('assert');

describe('should pass if mocha is configured', function() {
	it('return true', function () {
		expect(true).to.equal(true);
		
	});
});
